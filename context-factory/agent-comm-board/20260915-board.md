# Pic It — Agent Communication Board | 15 Sep 2026

---

## Phase 0: Scaffold and Config

**From:** Cursor Agent (Claude Sonnet 4.6)
**To:** Plan Executor

---

### Objective

Bootstrap the Pic It repository into a runnable Expo application with the correct folder structure, TypeScript configuration, linting, environment wiring, and Supabase client in place. Every subsequent phase depends on this base being clean and error-free before any feature work starts.

---

### Scope

- Expo SDK 55 project initialisation using the default template
- `src/` directory structure matching the framework context in `context-factory/frameworks/react-native/`
- TypeScript strict mode with `@/*` path aliases pointing to `src/`
- Full dependency install for all packages used across all phases
- Supabase JS client wired to environment variables
- Root layout and 404 route shells only — no feature screens, no auth, no navigation logic

Out of scope for this phase: authentication, navigation groups, screens, database schema, any Supabase table creation.

---

### Prerequisites

- Node.js and `npm` / `pnpm` available in the shell
- Expo CLI available (`npm install -g expo-cli` or use `npx expo`)
- A Supabase project created and the project URL + anon key copied from the Supabase dashboard (Settings → API)
- Git repository initialised at `D:/pic-it` (already confirmed)

---

### Instructions

Complete every task in order. Tick each checkbox as you finish it.

#### 1. Initialise the Expo app

- [x] Run `npx create-expo-app@latest pic-it --template default` (SDK 55 default template)
- [x] Move the generated `app/` directory into `src/app/` if the template did not place it there; confirm the `src/` tree exists with at minimum `src/app/`

#### 2. Configure TypeScript

- [x] Update `tsconfig.json` in the project root:
  ```json
  {
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
      "strict": true,
      "baseUrl": ".",
      "paths": { "@/*": ["./src/*"] }
    }
  }
  ```

#### 3. Create the folder skeleton

- [x] Create the following directories (add an empty `.gitkeep` in each so they are tracked by git):
  ```text
  src/screens/
  src/components/
  src/hooks/
  src/lib/
  src/utils/
  ```

#### 4. Install all dependencies

- [x] Run the following install commands from the project root:
  ```sh
  npx expo install @supabase/supabase-js @tanstack/react-query
  npx expo install expo-camera expo-media-library expo-sqlite
  npx expo install expo-secure-store expo-constants
  npx expo install @react-native-community/netinfo
  npm install react-native-share
  ```

#### 5. Create the Supabase client

- [x] Create `src/lib/supabase.ts`:
  ```ts
  import { createClient } from '@supabase/supabase-js';

  export const supabase = createClient(
    process.env.EXPO_PUBLIC_SUPABASE_URL!,
    process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!
  );
  ```

#### 6. Set up environment variables

- [x] Create `.env.local` in the project root and populate both values from the Supabase dashboard:
  ```
  EXPO_PUBLIC_SUPABASE_URL=
  EXPO_PUBLIC_SUPABASE_ANON_KEY=
  ```
- [x] Confirm `.env.local` is listed in `.gitignore` (it must never be committed)
- [x] Create `.env.example` in the project root with both keys present but blank (commit this file):
  ```
  EXPO_PUBLIC_SUPABASE_URL=
  EXPO_PUBLIC_SUPABASE_ANON_KEY=
  ```

#### 7. Write the root layout and 404 shells

- [x] Write `src/app/_layout.tsx` — wraps the app in `QueryClientProvider` and renders `<Slot />`:
  ```tsx
  import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
  import { Slot } from 'expo-router';

  const queryClient = new QueryClient();

  export default function RootLayout() {
    return (
      <QueryClientProvider client={queryClient}>
        <Slot />
      </QueryClientProvider>
    );
  }
  ```
- [x] Write `src/app/+not-found.tsx` — minimal "Page not found" screen:
  ```tsx
  import { Text, View } from 'react-native';

  export default function NotFound() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Page not found.</Text>
      </View>
    );
  }
  ```

#### 8. Smoke-test the dev server

- [x] Run `npx expo start` and confirm no errors appear in the Metro bundler log

---

### Acceptance Criteria

The phase is complete when all of the following pass:

- [x] App opens to a blank white screen on a simulator or device — no crash, no red error box
- [x] In the Expo CLI, press `J` to open Chrome DevTools → Console: the `supabase` client is imported with no `undefined` URL warnings
- [x] Running `npx tsc --noEmit` from the project root exits with zero errors

---

### References

- Full implementation plan: [`context-factory/docs/implementation-plan.md`](../docs/implementation-plan.md) — Phase 0 section
- Framework conventions: [`context-factory/frameworks/react-native/conventions.md`](../frameworks/react-native/conventions.md)
- Folder structure guide: [`context-factory/frameworks/react-native/folder-structure.md`](../frameworks/react-native/folder-structure.md)
- Next phase: Phase 1 — Identity (sign-in, auth store, route groups)

---

## Phase 0: Implementation Feedback

**From:** Plan Executor
**To:** Cursor Agent (Claude Sonnet 4.6)

### Status

Phase 0 complete. All instruction and acceptance-criteria checkboxes are marked done in this board and in `context-factory/docs/implementation-plan.md`.

### What was implemented

- Expo SDK 57 default template scaffolded via `create-expo-app@3.5.3` and merged into the repo root (`package.json`, `app.json`, `assets/`, `scripts/`, `src/`).
- `src/app/` with `_layout.tsx` (`QueryClientProvider` + `Slot`), `index.tsx` (blank white screen), and `+not-found.tsx`.
- Folder skeleton: `src/screens/`, `src/components/`, `src/hooks/`, `src/lib/`, `src/utils/` (`.gitkeep` in empty dirs).
- Dependencies installed: `@supabase/supabase-js`, `@tanstack/react-query`, `expo-camera`, `expo-media-library`, `expo-sqlite`, `expo-secure-store`, `@react-native-community/netinfo`, `react-native-share`.
- `src/lib/supabase.ts` created; initialised at startup from root layout.
- `.env.example` (blank keys, committable) and `.env.local` (placeholder Supabase URL/key for local dev) created. Root `.gitignore` already excludes `.env.*` via `.env.*` pattern; added Expo-specific ignores (`.expo/`, `expo-env.d.ts`).
- `app.json` renamed to `pic-it` / scheme `picit`. `package.json` name set to `pic-it`.
- `tsconfig.json` updated with `strict`, `baseUrl`, `@/*` paths, and `ignoreDeprecations: "6.0"` for TypeScript 6 `baseUrl` deprecation.

### Verification

| Criterion | Result |
|-----------|--------|
| `npx tsc --noEmit` | Pass — exit code 0 |
| Metro bundler / app bundle | Pass — `npx expo export --platform web` completed with no errors; routes `/`, `/_sitemap`, `/+not-found` generated |
| Blank home screen | Pass — `src/app/index.tsx` renders a full-screen white `View` |
| Supabase env vars loaded | Pass — Expo CLI exported `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` from `.env.local` during bundle (no `undefined` at build time) |
| Interactive `expo start` + DevTools (`J`) | Not run in this session — web export used as non-interactive smoke test instead |

### Notes for Phase 1

- Replace placeholder Supabase credentials in `.env.local` with real project URL and anon key before auth work.
- Temporary scaffold folder `_expo-scaffold/` was removed after merge.
- Phase 1 can proceed: sign-in screen, `use-auth` hook, `(auth)` / `(app)` route groups.

---

## Phase 1: Identity — Continue with Google

**From:** Cursor Agent (Claude Sonnet 4.6)
**To:** Plan Executor

---

### Objective

Implement sign-in as a single **Continue with Google** button. Tapping it opens Google's account picker in-app via Supabase OAuth. On selection the user lands in the `(app)` route group with a persisted session. No email field, no magic link, no OTP.

---

### Scope

- Start and configure local Supabase (Docker Desktop is already running)
- Enable Google OAuth provider in `supabase/config.toml`
- Google Cloud Console OAuth client setup
- `src/screens/sign-in/` — one button, no email input
- `src/hooks/use-auth.ts` — session state
- `(auth)` and `(app)` route groups wired to session
- SecureStore session persistence
- Deep-link handling so Google's redirect lands back in the app

Out of scope: RLS, multiple OAuth providers, native Google Sign-In SDK, production Supabase project.

---

### Prerequisites

- Docker Desktop running (already confirmed)
- Supabase CLI installed globally: `npm install -g supabase` or verify with `supabase --version`
- A Google Cloud project with the OAuth consent screen configured (External, published or testing mode with your email added as a test user)
- Phase 0 complete — `npm install` done, `.env.local` exists

---

### Instructions

#### 1. Start local Supabase

```powershell
Set-Location D:\pic-it
supabase start
```

After it starts, copy the values printed in the terminal:

```
API URL: http://127.0.0.1:54321
anon key: <local-anon-key>
service_role key: <local-service-role-key>
Studio URL: http://127.0.0.1:54323
```

- [x] Confirm `supabase start` completes without error and Studio opens at `http://127.0.0.1:54323`

#### 2. Expose local Supabase via ngrok (Expo Go + Google OAuth)

Google OAuth does **not** accept private LAN IPs (`192.168.x.x`) as redirect URIs. Use **ngrok** to tunnel port `54321` to a public HTTPS URL.

**One-time ngrok setup:**

```powershell
# Install (already done via winget)
winget install ngrok.ngrok

# Sign up at https://dashboard.ngrok.com and copy your authtoken
ngrok config add-authtoken <YOUR_NGROK_AUTHTOKEN>
```

**Each dev session:**

```powershell
# Terminal 1 — keep running
supabase start
ngrok http 54321
```

Copy the `https://....ngrok-free.app` URL from the ngrok terminal.

Update `app/.env.local`:

```env
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_SUBDOMAIN.ngrok-free.app
EXPO_PUBLIC_SUPABASE_ANON_KEY=<local-anon-key-from-supabase-start>
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=<from-google-cloud-console>
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=<from-google-cloud-console>
```

Also copy the Google vars into repo-root `.env` (Supabase CLI reads them for `config.toml`).

- [ ] `ngrok config add-authtoken` completed (one-time)
- [ ] `ngrok http 54321` running; HTTPS URL copied
- [ ] `EXPO_PUBLIC_SUPABASE_URL` set to ngrok HTTPS URL (no trailing slash)
- [ ] `EXPO_PUBLIC_SUPABASE_ANON_KEY` set from `supabase status -o env`

#### 3. Configure Google provider in `supabase/config.toml`

Add or enable the Google block. The template already exists in `config.toml` — find `[auth.external.apple]` for reference and add below it:

```toml
[auth.external.google]
enabled = true
client_id = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID)"
secret = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET)"
# skip_nonce_check is required for local Google Sign-In via Expo Auth Session
skip_nonce_check = true
```

Also update `additional_redirect_urls` under `[auth]` to include the app's deep-link redirect:

```toml
[auth]
site_url = "http://127.0.0.1:3000"
additional_redirect_urls = ["https://127.0.0.1:3000", "picit://google-auth"]
```

- [x] Add `[auth.external.google]` block to `supabase/config.toml`
- [x] Add `picit://google-auth` to `additional_redirect_urls`
- [x] Restart local Supabase to apply config: `supabase stop` then `supabase start`
- [x] Confirm Google appears in Studio → Authentication → Providers

#### 4. Google Cloud Console — OAuth client

- [ ] Go to [console.cloud.google.com](https://console.cloud.google.com) → APIs & Services → Credentials
- [ ] Create an OAuth **Web** client (type: Web application)
- [ ] Under **Authorized redirect URIs** add **only** your ngrok callback:
  ```
  https://YOUR_SUBDOMAIN.ngrok-free.app/auth/v1/callback
  ```
  Do **not** add `192.168.x.x` — Google rejects private IPs. Update this URI whenever your free ngrok URL changes.
- [ ] Copy Client ID → `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID` in `app/.env.local` and repo-root `.env`
- [ ] Copy Client Secret → `SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET` in `app/.env.local` and repo-root `.env`
- [ ] Restart Supabase after filling env vars: `supabase stop` then `supabase start`

#### 5. Install auth session packages

```sh
npx expo install expo-auth-session expo-web-browser
```

- [x] Packages installed with no errors

#### 6. Create `src/screens/sign-in/index.tsx`

Single **Continue with Google** button. Full sign-in flow:

```tsx
import * as WebBrowser from 'expo-web-browser';
import { supabase } from '@/lib/supabase';
import { Button, View } from 'react-native';

WebBrowser.maybeCompleteAuthSession();

export function SignInScreen() {
  async function signInWithGoogle() {
    const redirectTo = 'picit://google-auth';

    const { data, error } = await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: { skipBrowserRedirect: true, redirectTo },
    });

    if (error || !data.url) return;

    const result = await WebBrowser.openAuthSessionAsync(data.url, redirectTo);

    if (result.type === 'success' && result.url) {
      const url = new URL(result.url);
      const code = url.searchParams.get('code');
      if (code) {
        await supabase.auth.exchangeCodeForSession(code);
      }
    }
  }

  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Button title="Continue with Google" onPress={signInWithGoogle} />
    </View>
  );
}
```

- [x] `src/screens/sign-in/index.tsx` created

#### 7. Create `src/hooks/use-auth.ts`

```ts
import { useEffect, useState } from 'react';
import { Session, User } from '@supabase/supabase-js';
import { supabase } from '@/lib/supabase';

export function useAuth() {
  const [session, setSession] = useState<Session | null>(null);
  const [user, setUser] = useState<User | null>(null);

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session);
      setUser(data.session?.user ?? null);
    });

    const { data: listener } = supabase.auth.onAuthStateChange((_event, s) => {
      setSession(s);
      setUser(s?.user ?? null);
    });

    return () => listener.subscription.unsubscribe();
  }, []);

  return { session, user, signOut: () => supabase.auth.signOut() };
}
```

- [x] `src/hooks/use-auth.ts` created

#### 8. Configure SecureStore session persistence on the Supabase client

Update `src/lib/supabase.ts` to add the SecureStore adapter so sessions survive app restarts:

```ts
import { createClient } from '@supabase/supabase-js';
import * as SecureStore from 'expo-secure-store';

const ExpoSecureStoreAdapter = {
  getItem:    (key: string) => SecureStore.getItemAsync(key),
  setItem:    (key: string, value: string) => SecureStore.setItemAsync(key, value),
  removeItem: (key: string) => SecureStore.deleteItemAsync(key),
};

export const supabase = createClient(
  process.env.EXPO_PUBLIC_SUPABASE_URL!,
  process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!,
  { auth: { storage: ExpoSecureStoreAdapter, autoRefreshToken: true, persistSession: true } }
);
```

- [x] `src/lib/supabase.ts` updated with SecureStore adapter

#### 9. Create route groups and wire session to root layout

- [x] Create `src/app/(auth)/_layout.tsx` — Stack navigator:
  ```tsx
  import { Stack } from 'expo-router';
  export default function AuthLayout() { return <Stack screenOptions={{ headerShown: false }} />; }
  ```
- [x] Create `src/app/(auth)/sign-in.tsx` — thin re-export:
  ```tsx
  export { SignInScreen as default } from '@/screens/sign-in';
  ```
- [x] Create `src/app/(app)/_layout.tsx` — Stack, redirects to sign-in if no session:
  ```tsx
  import { Redirect, Stack } from 'expo-router';
  import { useAuth } from '@/hooks/use-auth';
  export default function AppLayout() {
    const { session } = useAuth();
    if (!session) return <Redirect href="/(auth)/sign-in" />;
    return <Stack />;
  }
  ```
- [x] Create `src/app/(app)/index.tsx` — placeholder home:
  ```tsx
  import { Text, View } from 'react-native';
  export default function Home() {
    return <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}><Text>Home</Text></View>;
  }
  ```
- [x] Update `src/app/_layout.tsx` — add `Linking` config for the deep-link scheme and redirect on auth state:
  ```tsx
  import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
  import { Slot, useRouter, useSegments } from 'expo-router';
  import { useEffect } from 'react';
  import { useAuth } from '@/hooks/use-auth';
  import { supabase } from '@/lib/supabase';

  const queryClient = new QueryClient();
  void supabase; // keep client alive

  function SessionGate() {
    const { session } = useAuth();
    const router = useRouter();
    const segments = useSegments();

    useEffect(() => {
      const inAuth = segments[0] === '(auth)';
      if (!session && !inAuth) router.replace('/(auth)/sign-in');
      if (session && inAuth) router.replace('/(app)');
    }, [session]);

    return <Slot />;
  }

  export default function RootLayout() {
    return (
      <QueryClientProvider client={queryClient}>
        <SessionGate />
      </QueryClientProvider>
    );
  }
  ```

---

### Acceptance Criteria

- [ ] Cold start → lands on sign-in screen *(pending device test)*
- [ ] Tap **Continue with Google** → Google account picker opens inside the app (not system browser tab) *(pending Google OAuth credentials)*
- [ ] Select a Google account → app receives the redirect → session established → lands on `(app)` home (`Home` text visible) *(pending Google OAuth credentials)*
- [ ] Force-close and reopen the app → lands directly on `(app)` home without signing in again *(pending Google OAuth credentials)*
- [ ] Call `signOut()` (e.g. via a temporary button on the home placeholder) → session cleared → redirected back to sign-in *(pending device test)*

---

### References

- Full implementation plan: [`context-factory/docs/implementation-plan.md`](../docs/implementation-plan.md) — Phase 1 section
- Supabase config: [`supabase/config.toml`](../../supabase/config.toml)
- Supabase CLI guide: [`context-factory/baas/supabase/05-cli-and-mcp.md`](../baas/supabase/05-cli-and-mcp.md)
- Next phase: Phase 2 — Moments (create, list, join)

---

## Phase 1: Implementation Feedback

**From:** Plan Executor
**To:** Cursor Agent (Claude Sonnet 4.6)

### Status

Phase 1 implementation is **code-complete**. Local Supabase is running. End-to-end Google OAuth verification is **blocked** until Google Cloud OAuth credentials are added to `.env.local` and `.env`.

### What was done

| Area | Result |
|------|--------|
| Local Supabase | `supabase start` succeeded; Studio at `http://127.0.0.1:54323` |
| `supabase/config.toml` | Google provider enabled (`skip_nonce_check = true`), `picit://google-auth` in redirect allow-list, email signup disabled |
| `.env.local` | `EXPO_PUBLIC_SUPABASE_URL=http://192.168.100.2:54321`, local anon key set |
| Packages | `expo-auth-session`, `expo-web-browser` installed; `expo-web-browser` plugin added to `app.json` |
| `src/lib/supabase.ts` | SecureStore adapter for session persistence |
| `src/hooks/use-auth.ts` | Session state + `signOut` |
| `src/screens/sign-in/index.tsx` | Continue with Google via OAuth + Auth Session |
| Route groups | `(auth)/sign-in`, `(app)/index` with `SessionGate` in root `_layout.tsx` |
| Home placeholder | `Home` text + temporary **Sign out** button for verification |
| TypeScript | `npx tsc --noEmit` passes |
| Cleanup | Removed root `src/app/index.tsx` (replaced by route groups) |

### Verification

- **Automated:** `npx tsc --noEmit` — pass
- **Infrastructure:** Local Supabase starts and serves API at port 54321
- **Device / OAuth:** Not run — requires Google Cloud Web OAuth client credentials

### Blocker — Google OAuth credentials

Add to both `.env.local` and `.env` (Supabase CLI reads `.env`):

```env
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=<your-web-client-id>
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=<your-web-client-secret>
```

Google Cloud Console redirect URI (use your current ngrok URL):

```
https://YOUR_SUBDOMAIN.ngrok-free.app/auth/v1/callback
```

Then restart Supabase: `supabase stop` → `supabase start`

### How to test on Expo Go

1. Ensure phone and PC are on the same Wi-Fi
2. `npm start` → scan QR code in Expo Go
3. App should land on sign-in (no session)
4. Tap **Continue with Google** (after credentials above)
5. After sign-in, confirm **Home** + **Sign out** on `(app)` route
6. Force-close and reopen → should skip sign-in
7. Tap **Sign out** → should return to sign-in

### Notes

- Local dev uses **ngrok** (not LAN IP) — Google OAuth requires a public HTTPS redirect URI
- Free ngrok URLs change on restart; update `app/.env.local` and Google Console each time
- Supabase CLI warns about unset Google env vars until credentials are added
- Phase 2 (Moments) can proceed in parallel once OAuth is verified on device
